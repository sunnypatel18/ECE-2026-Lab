vepo = mkvepo([2,1,0.8],[730,1090,2440],[0.1*pi, -0.3*pi, 0.2*pi],[4000,2000,8000],[1000,800,1200],8000,0.015);
ssa = vepo;
vepo = mkvepo([2,1,0.8],[530,1840,2480],[0.1*pi, -0.3*pi, 0.2*pi],[4000,2000,8000],[1000,800,1200],8000,0.015);
sse = vepo;
vepo = mkvepo([2,1,0.8],[270,2290,3010],[0.1*pi, -0.3*pi, 0.2*pi],[4000,2000,8000],[1000,800,1200],8000,0.015);
ssi = vepo;
vepo = mkvepo([2,1,0.8],[570,840,2410],[0.1*pi, -0.3*pi, 0.2*pi],[4000,2000,8000],[1000,800,1200],8000,0.015);
sso = vepo;
vepo = mkvepo([2,1,0.8],[300,870,2240],[0.1*pi, -0.3*pi, 0.2*pi],[4000,2000,8000],[1000,800,1200],8000,0.015);
ssu = vepo;

dur =2;
len=length(ssa);
overlap=len-96;
v1=vepo(1:96);
v=ssa(97:len)+ssa(1:overlap);
vv=[v,ssa(overlap+1:overlap + 96)];
cc=[];
for k=1:ceil(dur*f0)
    cc=[cc,vv];
end

soundsc([ssa sse ssi sso ssu]);